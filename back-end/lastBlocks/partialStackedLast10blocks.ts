// every minute cron job
// keep current burn block and partial stacked by cycle
// if new burn block -> verify if partial stacked by cycle > 0 && different than local partial stacked by cycle
// call increase and save partial stacked by cycle and burn block height
